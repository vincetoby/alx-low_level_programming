#include "main.h"

/**
 * print_rev - prints a string, in reverse
 * @s: string to be printed
 * Return: void
 */

void reverse_array(int *a, int n)
{
	for (i = 0; i < n - 1; i++)
		count++;
	for (j = i + 1; j >= 0; j--)
		count--;
}
