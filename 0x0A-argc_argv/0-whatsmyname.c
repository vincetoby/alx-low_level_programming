#include <stdio.h>

/**
 * main -  entry point that writes its name
 * @argc: arguement count
 * @argv: pointer to arguement strings
 * Return: 0
 **/
int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
